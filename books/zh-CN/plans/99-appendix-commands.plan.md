# 附录写作计划

Spec: `../specs/99-appendix-commands.spec.md`

- [x] 按安装诊断、检索、context、knowledge、cards、phase3、cowork、维护发布分类。
- [x] 只保留本书正文会用到的高频路径。
- [x] 使用 `AGENTS.md`、runbooks 和已完成 specs 校对命令名。
- [x] 使用 `target/debug/mempal <command> --help` 校对 lifecycle、card、
      evaluator、wake-up 命令参数。
- [x] 避免复制完整 CLI 帮助，附录只做操作速查。
- [x] 标注哪些命令默认 dry-run，哪些需要 `--execute`。
- [x] 覆盖 MCP tool 名称，便于 agent 侧对照。
- [x] 保持附录短而实用。
