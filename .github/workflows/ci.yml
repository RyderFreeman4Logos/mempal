name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  fmt:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Install stable Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt

      - name: Cache cargo registry
        uses: Swatinem/rust-cache@v2
        with:
          cache-targets: "false"

      - name: cargo fmt --all --check
        run: cargo fmt --all --check

  check:
    name: ${{ matrix.label }}
    needs: fmt
    runs-on: ubuntu-latest
    env:
      CARGO_BUILD_JOBS: ${{ matrix.cargo_build_jobs }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - label: default
            cargo_args: ""
            cargo_build_jobs: 2
          - label: rest
            cargo_args: "--features rest"
            cargo_build_jobs: 1
    steps:
      - uses: actions/checkout@v5

      - name: Install stable Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy

      - name: Cache cargo registry
        uses: Swatinem/rust-cache@v2
        with:
          cache-targets: "false"

      - name: cargo clippy --workspace --all-targets ${{ matrix.cargo_args }} -- -D warnings
        run: cargo clippy --workspace --all-targets ${{ matrix.cargo_args }} -- -D warnings

      - name: Clear clippy artifacts before tests
        run: cargo clean

      - name: cargo test --workspace ${{ matrix.cargo_args }}
        if: matrix.label == 'default'
        run: cargo test --workspace ${{ matrix.cargo_args }}

      - name: cargo test --workspace --features rest in target batches
        if: matrix.label == 'rest'
        run: bash scripts/ci/rest-tests.sh

      - name: Clear test artifacts before release build
        run: cargo clean

      - name: cargo build --workspace --release ${{ matrix.cargo_args }}
        run: cargo build --workspace --release ${{ matrix.cargo_args }}
